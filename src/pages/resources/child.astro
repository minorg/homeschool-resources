---
import { getCollection, getEntry } from "astro:content";
import ResourcesPage from "@/components/ResourcesPage.astro";

const page = (await getEntry("pages", "resources/child"))!;

const resources = await getCollection(
    "resources",
    ({ data }) => data.audience === "child",
);
---

<ResourcesPage resources={resources} {...page.data} />
